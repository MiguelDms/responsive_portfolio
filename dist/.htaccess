# mod_rewrite url start here 

RewriteEngine on

# CONDIÇÕES

# verificação para duplicação de links em pastas. Descarta tudo o que tiver o mesmo valor que um link, que não seja um # link 

RewriteCond %{REQUEST_FILENAME} !-d

# Se o url inserido não existir, então vai ignorar a rule

RewriteCond %{REQUEST_FILENAME}\.html -f

# REGRAS

#  mostramos aqui a página que contem o html 
#  ^(.*)$ significa tudo, ou o url todo é permitido, o $ termina a expressão regex
#  $1 imagino que significa a expressão regex numero um. O .html está então a acrescentar o sufixo
# [NC,L] são dois comandos para a rule. NC faz com que não haja distinção de Case. L termina as condições acima #especificadas na rule em questão

RewriteRule ^(.*)$ $1.html [NC,L]